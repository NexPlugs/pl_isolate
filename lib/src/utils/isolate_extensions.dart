import 'package:pl_isolate/pl_isolate.dart' show IsolateHelper;

extension IsolateHelperExt on IsolateHelper {
  //TODO: something with the isolate helper, maybe add some common methods or properties
}
